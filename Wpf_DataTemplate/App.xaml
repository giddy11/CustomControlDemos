<Application x:Class="Wpf_DataTemplate.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Wpf_DataTemplate"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <DataTemplate x:Key="MyTemplate1">
            <Label Content="{Binding Name}" Foreground="red" FontWeight="Bold"/>
        </DataTemplate>

        <!--<Style TargetType="ContentPresenter" x:Key="MyStyle1">
            <Setter Property="ContentTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <Label Content="{Binding Name}" Foreground="red" FontWeight="Bold"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>-->
    </Application.Resources>
</Application>
